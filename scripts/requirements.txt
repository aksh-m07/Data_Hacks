# Install into project venv only (do not pip install globally): python3 -m venv .venv && .venv/bin/pip install -r scripts/requirements.txt
numpy>=2.0.0
pandas>=2.2.0
requests==2.32.5
certifi>=2024.0.0
xgboost>=2.0.0
onnxmltools>=1.12.0
skl2onnx>=1.17.0
onnx>=1.16.0
onnxruntime>=1.16.0
